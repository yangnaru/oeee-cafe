<form id="post-edit-container"
      hx-put="/posts/{{ post_id }}"
      hx-target="this"
      hx-swap="outerHTML"
      hx-disable="find button">
    <div class="form-group">
        <label>{{ ftl_get_message("post-title") }}:</label>
        <input type="text" name="title" value="{{ post.title }}">
    </div>
    <div class="form-group">
        <label>{{ ftl_get_message("post-description") }}:</label>
        <textarea name="content">{{ post.content }}</textarea>
    </div>
    <div class="form-group">
        <label for="post-edit-hashtags">{{ ftl_get_message("post-hashtags") }}:</label>
        {# Same cap the publish form has always carried. Without it a long
           enough line was a failed edit rather than a truncated tag. #}
        <input type="text"
               id="post-edit-hashtags"
               name="hashtags"
               value="{{ hashtags }}"
               maxlength="500"
               aria-describedby="post-edit-hashtags-hint"
               placeholder="{{ ftl_get_message("post-hashtags-placeholder") }}">
        <span id="post-edit-hashtags-hint" class="form-hint">{{ ftl_get_message("post-hashtags-hint") }}</span>
    </div>
    <div class="form-group form-group-checkbox">
        <input type="checkbox"
               name="is_sensitive"
               id="is_sensitive"
               {% if post.is_sensitive == "true" %}checked{% endif %}>
        <label for="is_sensitive">{{ ftl_get_message("sensitive") }}</label>
    </div>
    <div class="form-group form-group-checkbox">
        <input type="checkbox"
               name="allow_relay"
               id="allow_relay"
               {% if post.allow_relay == "true" %}checked{% endif %}>
        <label for="allow_relay">{{ ftl_get_message("allow-relay") }}</label>
    </div>
    <div class="form-group form-group-checkbox">
        <input type="checkbox"
               name="allow_replay"
               id="allow_replay"
               {% if post.allow_replay == "true" %}checked{% endif %}>
        <label for="allow_replay">{{ ftl_get_message("allow-replay") }}</label>
    </div>
    <div class="form-actions">
        <button type="button" class="btn" hx-get="/posts/{{ post_id }}" hx-target="#post-edit-container" hx-swap="outerHTML">{{ ftl_get_message("cancel") }}</button>
        <button type="submit" class="btn">{{ ftl_get_message("save") }}</button>
    </div>
</form>
