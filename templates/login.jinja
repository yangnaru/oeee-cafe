{% extends "base.jinja" %}
{% block content %}
  <div class="center">
    <form method="post">
      <fieldset>
        <legend>{{ ftl_sign_in }}</legend>
        <p>
          <label for="login_name">{{ ftl_login_name }}</label>
          <input name="login_name" id="login_name" />
        </p>
        <p>
          <label for="password">{{ ftl_password }}</label>
          <input name="password" id="password" type="password" />
        </p>
      </fieldset>
      <input type="submit" value="{{ ftl_sign_in }}" />
      {% if next %}<input type="hidden" name="next" value="{{ next }}" />{% endif %}
    </form>
  </div>
{% endblock content %}
