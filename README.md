# oeee-cafe

## Development

```
$ cargo install bacon
$ bacon run

$ brew install caddy
$ caddy run -w

$ pnpm install vite

$ cd neo-cucumber
$ pnpm run dev
```

## Migration

```
$ cargo install sqlx-cli
$ sqlx migrate run
```
